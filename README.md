# Pybind <-> C++ interop pre-interview task

Takes a PIL image, zeroes out the bottom bits, and returns the image. Compile
with cmake (`cmake -B build && cmake --build build`), then run ./main.py to
demo.
